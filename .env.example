# Path to your 60-day Excel tracker (from ChatGPT earlier or your own path)
TRACKER_PATH=/absolute/path/to/career_autopilot_tracker.xlsx

# Optional: set a folder for generated apply packs
APPLY_PACK_DIR=./data/applications

# (Optional) LLM keys if you wire models later
OPENAI_API_KEY=sk-...
